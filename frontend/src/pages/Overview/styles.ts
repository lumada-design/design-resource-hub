import { css } from "@emotion/css";
import { theme } from "@hitachivantara/uikit-styles";

export const styles = {
  container: css({
    paddingTop: theme.spacing(6),
    minHeight: "100vh",
  }),
};
