import { css } from "@emotion/css";

const styles = {
  suspense: css({
    display: "flex",
    minHeight: `calc(100vh - 100px)`,
  }),
  layout: css({
    minHeight: `calc(100vh - 100px)`,
  }),
};

export default styles;
