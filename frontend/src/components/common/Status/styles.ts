import { css } from "@emotion/css";

const styles = {
  root: css({
    width: "100%",
  }),
  status: css({
    display: "flex",
    width: "100%",
    height: "100%",
    alignItems: "center",
  }),
};

export default styles;
